namespace WebApi
{
    public class DBCreds
    {
        internal static readonly string connectionString = "server = localhost; " +
                                                             "userid = root; " +
                                                            "password = Abhi@1214; " +
                                                             "database = testinglab";
        //internal static readonly string ConnectionString ="server = localhost; userid = root; password = Abh1Ank1; database = TrainingLab";
        //internal static readonly string ConnectionString ="server = localhost; userid = root; password = frontend; database = testinglab";
        //internal static readonly string ConnectionString = "server = traininglab.cwnie53k7o0m.ap-south-1.rds.amazonaws.com; userid = root; password = Prespectify; database = TestingLab";
    }
}
